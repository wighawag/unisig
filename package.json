{
  "name": "unisig-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Universal signals monorepo",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "format": "pnpm -r format",
    "format:check": "pnpm -r format:check",
    "build": "pnpm -r build",
    "test": "pnpm -r --reporter=append-only --reporter-hide-prefix test ",
    "bench": "pnpm -r bench",
    "dev": "pnpm --filter ./examples/svelte dev"
  }
}
