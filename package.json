{
  "name": "unisig-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Universal signals monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter svelte dev"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}